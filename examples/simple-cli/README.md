# Simple CLI

A very basic example of using the `simple_cli` custom component to create a CLI in your application. See the [README](../shared_components/simple_cli/README.md) in the component for details on usage.
